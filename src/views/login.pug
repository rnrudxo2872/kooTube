extends partials/base

block content
    if errorMessage 
        span=errorMessage
    form(method="post") 
        input(placeholder="Email",type="text", name="email")
        input(placeholder="Password",name="password",type="password",required)
        input(type="submit", value="로그인")
    hr
    div 
        span 계정이 없으신가요? 
        a(href="/join") 계정 만들기&rarr;