mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        ul
            li #{video.meta.rating}/5.
            li #{video.comments}개의 댓글
            li 게시날짜 #{video.createdAt}
            li #{video.meta.views}회

            